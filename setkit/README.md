# setkit

Concert Analytics storytelling and analysis helper package.

`setkit` is intended for reusable visualization themes, chart builders, metrics, and notebook helpers that emerge from On the Setlist project work.

Database access belongs in `loadin`, not `setkit`.
